# デザイン改善提案 2026-08 — 現物レビューに基づく次の一手

*2026-08-09 作成。代表的な誌面・表紙・前付け・プレビュー生成物を実際にレンダリングして確認し、「今より良くする方法」と「追加でやるべき内容候補」をまとめた提案書です。実装（CSS/Astro/React）は別担当のスコープなので、本書は「仕様・検証・原稿側で何をするか」と「実装担当へ何を渡すか」に分けて書いています。*

---

## 0. 何を見たか（エビデンス）

| 確認対象 | 方法 | 結果 |
|---|---|---|
| 見開きプレビュー B-3 / I-1 / E-1 / J-14 | Chromium で実レンダリング・スクリーンショット | 高い完成度。ポンチ絵実画像・字数バッジ・前後ナビが機能 |
| 実寸 750×1061px への収まり | `.page` の scrollHeight 実測（10 件サンプル） | **オーバーフロー 0 件**（E-1 の旧 +43px 課題は解消済みと判断） |
| 表紙 `drafts/covers/vibe-coding-cover-rich.html` | 実レンダリング + CSS 確認 | **OS フォント（Yu Gothic / Arial）依存**。フォント未搭載環境でタイトルがクリップ |
| 前付け `front_section/0_concept_spread.html` | 実レンダリング | 構成は良好。イラスト枠 13 枠が未差し込み（IMAGE_GEN_TODO のまま） |
| 出典メモの URL 有無 | 全 md を機械走査 | ready / needs_review のうち **65 件が出典メモに URL なし**（例: J-14 は誌面に「参考（未記入）checked —」がそのまま出る） |
| 時変情報の鮮度 | evaluation_date 走査 | version_status / pricing_note を持つのに **評価日が 2026-05 以前のものが 305 件**（本日 2026-08） |
| 要直しキュー | `ledgers/revision_queue.md` | ☆ 違反 0 / **⚠️ 139 件（大半が reader_level の幅ルール）** |

現状ステータス: ready 252 / needs_review 103 / drafting 1 / archived 8（スケルトンは実質ゼロ。執筆フェーズはほぼ完走）。

## 1. 現状評価 — 誌面デザインは「凍結時より進化して」おり、質は高い

- 見開きは iter 22 以降さらに反復され、メイン図 1 枠化・開発フロー左ページ化・最終ポンチ絵（webp 370 枚）差し込みまで進んでいます。誌面としての説得力は書籍見本レベルです。
- プレビュー生成器の開発ツールバー（節ごとの字数バッジ・status メダル・物理ページ番号）は執筆と検証の回転を良くしており、この投資は正解でした。
- 前付け・裏表紙・検索インデックス・管理トップ・Cloudflare Pages と、書籍の「周辺」も育っています。

つまり「デザインを大きく変える」フェーズではなく、**（a）仕様と実装のズレを締め直し、（b）誌面に出てしまう品質バグを潰し、（c）印刷・公開に向けた検証を自動化する**フェーズです。

## 2. 改善提案（優先度順）

### P1: 仕様書と現行レイアウトのドリフト解消（実装引き渡しのブロッカー）

`docs/v2_rules_summary.md` §1（2026-04-25 凍結）と現行プレビューが食い違っています。

| 項目 | 仕様書の記述 | 現行プレビューの実態 |
|---|---|---|
| 開発フローでの位置 | 右ページ（6 視点の下） | **左ページ下段**（メイン図の下） |
| メイン図 | Before/After 図 + 擬人化ポンチ絵の 2 枠 | **「イメージ」1 枠に統合**（最終画像差し込み済み） |
| 右ページ下段 | 開発フロー → 関連用語 | 関連用語 → **備考カード** → 参考 URL |
| E-1 +43px 残（§7） | 未解決として記載 | 実測で解消済み |

Astro + React の実装担当は `v2_rules_summary.md` と `component_spec_v2.md` を単一真実点として渡される前提なので、このまま渡すと**古いレイアウトが実装されます**。

**やること**: 現行プレビュー（`scripts/preview_gen.py` の出力）を正として v2.29 を起こし、`v2_rules_summary.md` §1・§4、`component_spec_v2.md`、`entry_schema.yaml` の 3 点を同期。§7 の解消済み項目もクローズする。あわせて `CLAUDE.md` §2（「本書き 14 件」のまま）と `ledgers/next_session_handoff.md`（v11 = 5/23 のまま）も現状に更新する。

### P2: 誌面に出てしまう品質バグの一掃（原稿側・機械化可能）

1. **参考 URL 未記入 65 件**: ready なのに誌面へ「参考（未記入）checked —」が印字される状態。`validate_entry.py` に「ready / needs_review で出典メモに URL 0 本 → ☆ 違反」を追加し、65 件を棚卸しする（人物・国産 YouTuber 系は公式チャンネル URL で埋まるはず）。
2. **⚠️ 139 件の大半を占める reader_level 幅ルール**: 「幅 2 段以上（2-4 等）は警告」のルールと実データが噛み合っていません。方針を決めて一掃を推奨します。推奨は**データ側を寄せる**（2-4 → 中央 1 段幅へ）ですが、実態優先でルール側を「幅 2 まで許容」に緩める判断もあり得ます。どちらでも、ノイズが 139 件あると本当の警告が埋もれるのが問題です。
3. **時変情報の鮮度 305 件**: モデル・料金を含むのに評価日が 3 ヶ月以上前。全件書き直しは不要で、まず「checked から N ヶ月経過で ⚠️」を revision_queue に出す仕組みを足し、刊行前に letter 単位で棚卸しする運用を提案します（B 料金系・D モデル系が優先）。

### P3: 表紙・前付けを本文と同じデザインシステムに載せる

- 表紙 4 種（cover-a / rich / sell / back）は **Yu Gothic / Arial 指定**で、本文の確定トークン（Zen Maru Gothic + Inter、#123E82 系）と別系統です。フォント未搭載環境（今回の Linux レンダリング）ではタイトル「バイブコーディング図鑑」がクリップしました。印刷入稿時のフォント埋め込みも現状ではできません。
  - **やること**: 表紙 CSS を本文と同じ webfont + カラートークンに載せ替える指示書を書き、実装担当へ渡す（`white-space: nowrap` 箇所のはみ出し検証込み）。
- 前付けのイラスト 13 枠は `drafts/IMAGE_GEN_TODO.md` に整理済みのまま止まっています。ポンチ絵 370 枚のパイプライン（プロンプト規約・色監査・品質スコア）が既にあるので、**同じパイプラインに前付け 13 枠を流す**のが最短です。

## 3. 追加でやるべき内容候補（バックログ提案）

効果と手間で並べています。上 3 つはこのプロジェクト（markdown + 検証）内で完結します。

1. **全 364 件の実寸オーバーフロー監査を自動化**（効果大・手間小）
   今回の実測はサンプル 10 件です。`preview_to_pdf.py` 相当の scrollHeight 実測を全件回すスクリプトを `scripts/` に常設し、revision_queue に「はみ出し」列を足す。将来は GitHub Actions で PR ごとに回すと、原稿を伸ばした瞬間に検知できます。
2. **関連用語ピルのリンク解決率監査**（効果中・手間小）
   related_terms が実在エントリ ID / タイトルに解決できるかを全件チェック。書籍では相互参照の破れ、Web 版ではリンク切れになる箇所を刊行前に把握します。
3. **章ごとの視覚変化点の仕様化**（効果中・手間中、§7 の積み残し）
   339 見開き同型の単調さ対策。新しい色を増やさずにできる範囲として、（i）章扉スプレッド 10 枚、（ii）チロムの章アイコン（現行のメダル絵柄の体系化）、（iii）小口側の章インデックス帯（爪掛け）の 3 案を推奨。仕様だけ固めて実装担当へ。
4. **Web 版の読者導線**（効果中・手間中）
   `drafts/index.html` は管理用の全ページ一覧で、読者向けの入口ではありません。`drafts/search/index.json` と `reading_routes` が既にあるので、「検索 → 見開き」「レベル別・ルート別の入口」を読者向けトップとして 1 枚に統合する価値があります。750px 固定幅なのでスマホでの閲覧体験（横スクロール）も要判断です（誌面はそのまま・ビューアで縮小、が現実的）。
5. **印刷前工程の消化**（効果大・時期依存）
   §7 の残り: bleed 3〜5mm、CMYK 色校正、Zen Maru Gothic の埋め込み確認。`build_galley_pdf.py` で束見本 PDF を定期生成し、ページ数（現状 physical page 番号で 570 頁前後まで振られています）と綴じの現実性を著者が確認するタイミングを一度作るのを推奨します。
6. **evaluation_date 更新スプリント**（効果大・手間大）
   P2-3 の仕組みを入れた後、B（料金）→ D（モデル）→ C（企業）の順で letter 単位に評価日を更新。entry-writer の並列運用ノウハウがそのまま使えます。

## 4. 収録語の抜け監査 — 「この言葉がないのはおかしい」追加候補

> **実施済み（2026-08-10）**：本節の ☆☆☆ 15 件・☆☆ 20 件をすべて執筆しました。確定 ID と提案時の仮 ID からの変更点は [ledgers/entry_candidates.md](../ledgers/entry_candidates.md) の「M 差分（2026-08-10）」、執筆に使った事実確認メモは [ledgers/stage3_briefs_2026-08.md](../ledgers/stage3_briefs_2026-08.md) にあります。なお F-43 テストは「欠番」ではなく、本書き済みのエントリが `archived` で凍結されていたため復活させました。

現行 364 件の全タイトルを letter 横断で走査し、想定読者（非エンジニアで AI 開発の会話に入る人）が 2026 年時点で高頻度に踏む語と突き合わせました。以下は **`ledgers/entry_candidates.md` にも未収載**のものだけです（台帳に既にある語は挙げていません）。仮 ID は `docs/id_scheme.md` の空き番号への仮割当てで、確定は著者判断です。

### ☆☆☆ 明らかな抜け（優先追加を推奨・15 件）

| 仮 ID | 用語 | なぜ「ないとおかしい」か |
|---|---|---|
| G-49 | AI エージェント | 2025〜26 の中心語。H-59「AI エージェント元年」や G-12「Agent Design」が前提にしているのに、本体の定義項目がありません |
| F-213 | API | 非エンジニアの最頻出つまずき語。F-212 OpenAPI や G-33 Function Calling が説明なしで使っています |
| F-214 | API キー | 「キーを発行して貼る」はバイブコーディングの最初の関門。漏えい注意（.env との接続）も書けます |
| F-45 | デプロイ | 「作ったものを公開する」動作の名前。F-42 ビルドはあるのに対になるデプロイがなく、B-20 Vercel 等が前提にしています |
| F-46 | デバッグ | AI との開発会話で毎日出る語。バグ／デバッグの定義項目がありません |
| F-43 | テスト | 候補台帳には当初からあるのに執筆から漏れ落ちています（F-42 ビルドの隣の欠番） |
| F-18 | フレームワーク／ライブラリ | React 等の説明文にも出てくるメタ語彙。この 2 語の区別は非エンジニアの定番のつまずきです |
| F-47 | フロントエンド／バックエンド | 開発会話の役割分担語。多くの見開きが暗黙に使っています |
| J-34 | マルチモーダル | 主要 3 サービス全部の売り文句で、J-15 VLM より先に読者が出会う語です |
| J-35 | スケーリング則 | モデル進化ニュースを読む鍵で、H 章の歴史記事の背骨にもなります |
| G-37 | プロンプトインジェクション | エージェントに仕事を任せる読者が最初に踏むリスク語。G-39 Permission の隣に置けます |
| E-5 | ARC-AGI | o3 発表以降、一般ニュースにも出る指標。E 章の「数字の読み方」路線に合います |
| B-34 | NotebookLM | 非エンジニア層に最も刺さっている Google ツールの一つで、B 章の想定読者ど真ん中です |
| B-35 | Gemini CLI | 無料で使える CLI エージェントとして Claude Code（B-7）／Codex（B-8）と並べて比較される存在です |
| B-36 or D-59 | Midjourney | 画像生成の一般名詞級サービスなのに B・D どちらにもありません（DALL-E / Stable Diffusion / Imagen は収録済み） |

### ☆☆ 検討したい（20 件）

| 領域 | 候補 | ひとこと |
|---|---|---|
| B サービス | Lovable / Replit / Manus / Google AI Studio / n8n・Dify / Discord | Bolt・v0 と並ぶバイブコーディング系（Lovable, Replit）、汎用エージェント（Manus）、無料入口（AI Studio）、非エンジニア向け自動化（n8n・Dify）、AI コミュニティの実際の居場所（Discord） |
| C 企業 | DeepSeek（企業）/ Sakana AI | モデル項目はあるのに企業項目がない DeepSeek、日本の読者向けに Sakana AI |
| D モデル | AlphaFold | AlphaGo（D-60）はあるのにノーベル賞の AlphaFold がない |
| G 使いこなし | Temperature / AI のメモリ機能 / Computer Use（ブラウザ・PC 操作） | 設定画面や機能名として読者が直接触る語 |
| J 一般 | 蒸留 / RLHF・アラインメント / NPU・AI PC / TPU / データセンターと電力 / 世界モデル / ムーアの法則 | DeepSeek 以降の報道語（蒸留）、PC 売り場の語（NPU）、2026 年の電力議論など |
| H 歴史 | DeepSeek ショック（2025-01） | 株価まで動かした一般ニュース級の転換点 |

### 運用

1. 採用分を `ledgers/entry_candidates.md` に M 差分として追記（ID 確定）
2. `python3 scripts/generate_skeleton.py {ID}` で単発生成 → entry-writer で本書き（既存の 5 並列運用がそのまま使えます）
3. 「関連用語ピルのリンク解決率監査」（§3-2）を先に回すと、既存エントリが参照しているのに存在しない語＝抜けの機械的な裏取りにもなります

## 5. 進め方の推奨（次の 1〜2 セッション）

1. **このセッションの成果物**: 本提案書のレビュー（著者判断が要るのは P2-2 の reader_level 方針と §3-3 の章変化点の採否）
2. **次セッション A（半日）**: P1 の仕様同期 + P2-1 の validator 強化と URL 65 件棚卸し
3. **次セッション B（半日）**: P2-2 の一掃 + §3-1 の全件オーバーフロー監査スクリプト
4. **実装担当への引き渡しパック**: 同期済み `v2_rules_summary.md` v2.29 + `component_spec_v2.md` + 表紙載せ替え指示書（P3）

---

*確認に使った一時スクリプト（Playwright 実測・URL 走査）は再現可能な形で本文に記載済みのため、リポジトリには含めていません。必要なら `scripts/` への常設化（§3-1）とセットで追加します。*
