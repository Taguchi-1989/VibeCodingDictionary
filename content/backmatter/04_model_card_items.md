---
id: back_model_card_items
title: モデルカードの読みどころ
title_reading: モデルカードノヨミドコロ
category: common
subtype: appendix
experience_level: research_only
reader_level: 3
importance: C
page_layout: back_checklist
evaluation_date: 2026-09-05
status: drafting
---

# モデルカードの読みどころ

巻末付録の 4 枚目です。前の 3 枚が「規格を見比べる」「導入の段を追う」ページなのに対して、こちらは **モデルを選ぶときに、何が書いてあるはずで、どこから読むのか**を 1 枚にします。

肝は 1 行です。**できることではなく、できないことが書いてある欄から読みます。**

本ファイルはデザイン担当向けのレイアウト指示書として扱います。左ページに 9 項目の表、右ページに読む順と本文を置きます。本編の J-143 モデルカードが「それが何か」を扱い、この付録は「どう読むか」に徹します。

<!-- ━━━━━━━━ 左ページ ━━━━━━━━ -->

## 左ページの表（9 項目）

### 表の狙い

Mitchell ら（2019 年）の "Model Cards for Model Reporting" が挙げる 9 項目を、**順番どおりに全部**並べます。読者が実物のモデルカードを開いたとき、目次として突き合わせられる状態にするのが目的です。

### 項目と中身

| 項目 | 中身 |
| :-- | :-- |
| モデルの詳細 | 誰が、いつ、どのバージョンを作ったか |
| **意図された用途** | **何のために作ったか。想定していない用途も書く** |
| 要因 | 性能が変わる条件（人・機材・環境など） |
| 指標 | どの物差しで測ったか |
| 評価データ | 何を使って測ったか |
| 学習データ | 何を使って学んだか |
| **定量分析** | **条件ごとの性能。平均は良くてもここだけ悪い、が見える** |
| 倫理的考慮 | 想定される害 |
| **注意と推奨** | **使う側への申し送り** |

### 表を作るときの注意

- **9 行を減らさないでください。** 実物と突き合わせる目次が役割なので、抜けがあると使えなくなります
- **太字の 3 行（意図された用途・定量分析・注意と推奨）だけ強く見せてください。** 非エンジニアが最初に読むべき欄です
- 項目名は原語（Intended Use など）を小さく添えると、英語のモデルカードでも引けます

## 実物はどう書かれているか

実際のモデルカードは、**リポジトリに置かれた 1 枚の `README.md`** です。上に **YAML の見出し（メタデータ）**、その下に **本文（9 項目に対応する文章）**、という 2 段構えになっています。

### 上段：YAML の見出し（機械が読む欄）

```yaml
---
library_name: transformers
pipeline_tag: text-generation      # このモデルは何をするものか
language:
  - ja
  - en
base_model:
  - org-name/base-model-name       # 何をもとに作ったか
license: apache-2.0
tags:
  - pytorch
---
```

**機械が読んで一覧・絞り込みに使う欄**です。用途・対応言語・ライセンス・派生元がここで分かります。**「どのモデルから派生したか（`base_model`）」は、責任の追跡（J-150 トレーサビリティ）の出発点**になります。

### 下段：本文（人が読む欄）

```markdown
## Model Details
作成者・公開日・バージョン・モデルの種類

## Intended Use
**想定する用途**：日本語の社内文書の要約
**想定しない用途**：医療・法務など、誤りが重大な結果を招く判断

## Factors
入力の長さ、専門用語の多さ、文書の形式で性能が変わります

## Metrics / Evaluation Data / Training Data
何を、何で測ったか

## Quantitative Analyses
条件ごとの結果（全体 82%、専門用語の多い文書 61% など）

## Ethical Considerations / Caveats and Recommendations
**長い文書では要約が抜け落ちることがあります。重要な判断の前に原文を確認してください**
```

### 読むときの目のつけどころ

- **`Intended Use` の「想定しない用途」**が、いちばん実務に効きます。ここに自分の用途が入っていたら、性能表を見るまでもありません
- **`Quantitative Analyses` の条件別**を見ます。全体の点数だけ高くて、自分が扱う条件で落ちている、というのがよくある形です
- **書式は揃っていません。** 9 項目すべてが埋まっているとは限らず、見出し名も統一されていません。**空欄そのものが情報**だと考えてください

<!-- ━━━━━━━━ 右ページ ━━━━━━━━ -->

## 右ページの図（読む順）

上から 1・2・3 と番号を振った矢印で、**意図された用途 → 注意と推奨 → 定量分析**の順に読むことを示します。性能表（指標・評価データ）は 4 番目に置きます。

## 読み方

モデルカードは、モデルの取扱説明書です。**性能の高さは各社が良く見せますが、「意図された用途」と「注意と推奨」は、作った側が自分で置いた制約**です。実務では、こちらのほうが効きます。

読む順は 3 つで足ります。**何のために作られたか**を見て、自分の用途がそこに入っているかを確かめます。次に**注意と推奨**を見て、避けるべき使い方を拾います。最後に**定量分析**を見て、自分が扱う条件で性能が落ちていないかを確かめます。平均点だけを見ていると、ここで落ちます。

**2026 年に位置づけが変わりました。** 任意のベストプラクティスから、規制上の要求へ移っています。高リスクの用途では技術文書が任意ではなくなり、モデルカードはその実体のひとつになりました。**「学習させたので大丈夫です」では通らず、どういう条件で、どこまでが限界かを示す**ことが求められます。

## 本編への案内

- **J-143 モデルカード** — この様式そのものの説明
- **J-139 説明可能性** — 個々の出力がなぜそうなったかを示す話。モデルカードはモデル全体の説明書で、層が違います
- **G-50 Evals** — 他人が測った点数ではなく、自分の仕事で測り直す側
- **J-58 EU AI Act** — 技術文書が要求になる背景
- **J-147 データ品質** — 学習データ・評価データの欄の裏側にある要求

<!-- ━━━━━━━━ 裏台帳メモ（誌面には出さない） ━━━━━━━━ -->

## 誌面ポンチ絵メモ

- 左ページ: 9 行 2 列の表。太字 3 行だけ地色を敷く。右端に小さくチェックボックスを置き、「実物と突き合わせる目次」であることを形で示す
- 右ページ: 上から番号つきの矢印 3 本（意図された用途 → 注意と推奨 → 定量分析）。4 番目に性能表を薄く置き、「そこは最後」と分かるようにする
- 登場人物: モデルカードを手にした非エンジニアが、性能表を飛ばして「注意と推奨」を指さしている

## 出典メモ

- Mitchell et al. "Model Cards for Model Reporting"（2019）<https://arxiv.org/pdf/1810.03993> — checked 2026-09-05
- <https://futureagi.com/glossary/model-cards/> — checked 2026-09-05
- <https://www.techaheadcorp.com/blog/ai-model-cards-data-provenance/> — checked 2026-09-05

## 備考

- **著者の指示（2026-09-05）で立てたページです。** 「9 項目の表はどこかに入れておきたい、これは必要な情報だ」という指示に対し、本編の見開き（spread_v1）には 9 行の表が入らないため、巻末付録として独立させました
- そのために `back_checklist` レイアウトを新設しています（`docs/back_section_layout.md` §4-3）。**back_matrix の「5〜6 行に収める」制限は、比較ではなく点検が目的のこのページには当てません**
- EU AI Act の適用時期は書いていません（段階施行中の時変情報）。「規制上の要求へ移った」という位置づけの変化までに留めています
